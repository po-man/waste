# Coastline Calc

Tiny scaffold for the Coastline Calc web app (React + Vite + TypeScript).

Quick start:

```bash
npm install
npm run dev
```

Run tests:

```bash
npm run test
```

This scaffold includes a simple polyline length calculator at `src/lib/calc.ts` and a minimal UI.
